<%= titulo("Planes") %> 
<p id="notice"><%= notice %></p>
<div class="panel-body2"  style="height: 95%;">
    <div class="row">
        <div class="col-md-4" >
          <%= link_to 'Nuevo', new_plan_path,
            class: "btn btn-default btn-sm nuevo_cliente"  %>
        </div>
        <div class="col-md-8" align="right">
           <%= buscar(planes_path,["nombre"]) %>
        </div>
    </div>
    <%= tabla_panel_responsive(["Nombres","Monto Semanal","Monto Mensual","Monto Semestral","Monto Anual"],@planes)%>
    <%= paginador(@planes,controller: :planes, action: :index ) %>
</div>
